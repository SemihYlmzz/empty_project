import 'package:flutter/services.dart';
import 'package:fpdart/fpdart.dart';

import '../common/common.dart';

class ImageService {
  ImageService(
      // required this.imageApi,
      );
  // final ImageApi imageApi;

  // FutureEither<Uint8List> pickSingleImageFromGallery() async {
  //   try {
  //     return Right(await imageApi.pickSingleImageFromGallery());
  //   } catch (exception) {
  //     if (exception is ImageException) {
  //       return Left(_imageExceptionTranslatedToUser(exception));
  //     }
  //     return Left(_imageExceptionTranslatedToUser(ImageUnknown()));
  //   }
  // }

  // FutureEither<Uint8List> pickSingleImageFromGallery() async {
  //   try {
  //     return Right(await imageApi.pickSingleImageFromGallery());
  //   } catch (exception) {
  //     if (exception is ImageException) {
  //       return Left(_imageExceptionTranslatedToUser(exception));
  //     }
  //     return Left(_imageExceptionTranslatedToUser(ImageUnknown()));
  //   }
  // }
}
