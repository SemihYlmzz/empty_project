import 'package:flutter/material.dart';
  
  class AppVibrationSetting extends StatelessWidget {
    const AppVibrationSetting({super.key});
  
    @override
    Widget build(BuildContext context) {
      return ElevatedButton(
        onPressed: () {},
        child: const Text('AppVibrationSetting'),
      );
    }
  }
