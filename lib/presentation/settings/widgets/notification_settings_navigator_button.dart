import 'package:flutter/material.dart';
  
  class NotificationSettingsNavigatorButton extends StatelessWidget {
    const NotificationSettingsNavigatorButton({super.key});
  
    @override
    Widget build(BuildContext context) {
      return ElevatedButton(
        onPressed: () {},
        child: const Text('NotificationSettingsNavigatorButton'),
      );
    }
  }
