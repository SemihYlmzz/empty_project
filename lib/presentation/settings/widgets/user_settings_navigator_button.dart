import 'package:flutter/material.dart';
  
  class UserSettingsNavigatorButton extends StatelessWidget {
    const UserSettingsNavigatorButton({super.key});
  
    @override
    Widget build(BuildContext context) {
      return ElevatedButton(
        onPressed: () {},
        child: const Text('UserSettingsNavigatorButton'),
      );
    }
  }
