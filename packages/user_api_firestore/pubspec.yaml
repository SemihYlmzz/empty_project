name: user_api_firestore
description: A very good flutter package
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  # User Api
  user_api:
    path: ../user_api

  # Firebase Auth
  cloud_firestore: ^4.15.4
  firebase_core: ^2.25.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  very_good_analysis: ^5.1.0
